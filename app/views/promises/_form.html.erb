<%= form_for @promise do |f|  %>

  <%= f.label "Title" %>
  <%= f.text_field :title %>

  <%= f.label "Promiser" %>
  <%= f.text_field :promiser %>

  <%= f.label "Status" %>
  <%= f.select(:status, [['in progress'], ['done'], ['abandoned']]) %>

  <%= f.submit %>
<% end %>
